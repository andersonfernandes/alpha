<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Alpha</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="javascript:void(0)" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <%= image_tag attachment_url(current_user, :profile_image, :fill, 35, 35), class: 'img-circle' if current_user.profile_image %>
              <%= current_user.name %>
              <span class="caret"> </span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t('.edit_user'), edit_user_registration_path(current_user) %></li>
              <li><%= link_to t('devise.sessions.sign_out'), destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t('devise.registrations.new.sign_up'), new_user_registration_path %></li>
          <li><%= link_to t('devise.sessions.new.sign_in'), new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
